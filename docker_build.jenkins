#!groovy
// Run docker build

pipeline {
    agent any

    stages {
        stage("create docker image") {
			steps {
				echo "---------- start building image ----------"
            			bat 'docker build .'
			}
        }
    }
}
