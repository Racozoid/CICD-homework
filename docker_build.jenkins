#!groovy
// Run docker build

pipeline {
    agent any
        
        stage("create docker image") {
			steps {
		        echo "---------- start building image ----------"
                bat 'docker build -t racozoid/ci-cd-homework:latest .'
			}
        }
        
}
