#!groovy
// Run docker build
properties([disableConcurrentBuilds()])

pipeline {
    agent any

    options {
        timestamps()
    }

    stages {
        stage("create docker image") {
            echo "--------------------start building--------------------"
            sh 'docker build .'
        }
    }
}